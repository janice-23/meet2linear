{
  "name": "meet2linear",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "server",
    "extension"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "build:ext": "npm run build -w extension",
    "extract": "npm run extract -w server --",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
